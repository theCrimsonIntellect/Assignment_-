package com.pinkReasons;

import java.util.ArrayList;

public class MidnightPractice
{

    ArrayList<ArrayList<Double>> col = new ArrayList<>();
    ArrayList<Double> l = new ArrayList<>();


    public ArrayList<Double> findSubset(ArrayList<Double> shoppingList, double budget)
    {
        col.add(l);
        return null;

    }



    public double sum ( ArrayList<Double> list)
    {
        double sum = 0;

        for (int i = 0; i < list.size(); i++)
        {
            sum = sum + list.get(i);
        }

        return sum;
    }

}
